#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        vector<int> a(n);
        vector<int> a(n);
        for (int i = 0; i < n; i++) {
            cin >> a[i];
        }
        for (int i = 0; i < n; i++) {
            cin >> b[i];
        }
        int maxLen = 1;
        int al = 0; ar = n-1;
        int bl = 0; br = n-1;
        itnt currLen = 0;
        while(ar<n && br<n){
            
        }
        //1 1 9 1 5 4
        //1 2 5 7 5 4
        // Example usage:
        // print(arr);
    }
}